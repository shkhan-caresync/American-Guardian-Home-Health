# Public Assets

Place your logo file here:

**Logo file:** `american-guardian-logo.png`

The logo is referenced in the brand configuration at `src/config/brand.js` and used in:
- Navigation component
- Hero section
- Contact/Footer section

**Note:** The logo path is set to `/american-guardian-logo.png` which will be served from this `public` directory in most React frameworks (Create React App, Vite, etc.).

